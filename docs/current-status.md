# 现状汇总

> 截至 2026-07-30 晚间。这份文档回答两个问题：手上有哪些采集数据、能力闭环做到哪一步。
> 逐条能力的证据来源看[能力证据矩阵](capability-evidence.md)，这里只讲全局。

## 一句话

**代码层四层都齐了，已知清障页也已经融合进 `play`，闭环还没成立。** 一整圈里
需要动手的三段，大厅排队有实证，结算返回序列已经由程序真实发出；跳伞脱离仍因本次
人工跳伞而未验。`20260730-191304` 还暴露了一个公共故障：共享 RapidOCR 在快速识别后
没有重新开启文字框检测，令覆盖层安全门连续失败并挡住清障和大厅动作，现已修正。

「闭环接上了」这个说法在 2026-07-30 的对话里出现过，指的是**执行路径**接上了
（能力集第一次真的被执行），不是整圈跑通了。两件事别混。

## 采集数据台账

八份，全部来自同一台目标机器（`2560×1440`、中文、全屏）。

| 数据 | 类型 | 规模 | 从它身上得到了什么 |
|---|---|---|---|
| `Apex…20.29.49.05.DVR.mp4` | 录屏 4 分钟 60fps | — | 排队画面的第一版 ROI（后被实时帧推翻） |
| `20260728-234103` | 旧管线 tutorial | 204 事件 / 12 截图 / 541s | **训练场误按「准备」的铁证**；控制模式排队文字 |
| `20260729-224809` | observe | 174 帧 / 54 截图 / 575s | 结算页底部提示行的文字 |
| `20260729-230324` | observe | 738 帧 / 74 截图 / 580s | **现有全部 ROI 的标定来源** |
| `20260729-235005` | observe | 747 帧 / 144 截图 / 761s | 排队规则改对、控制模式的洞、模式卡片坐标、`LOBBY_READY_OTHER` |
| `20260730-005852` | observe | 547 帧 / 42 截图 / 394s | 确认识别层可用：15 个状态全部命中 |
| `20260730-010042` | **play**（首次真跑） | 327 帧 / 94 事件 / 发出 15 次输入 / 283s | 三个真 bug：训练场误按、近战键位、跳伞长按 |
| `20260730-191304` | **play** | 813 帧 / 207 事件 / 发出 29 次输入 / 452s | 自动继续、27 次近战、真实发出结算返回序列；定位覆盖层 OCR 连续 130 次缺框错误 |

原始全屏帧和 MP4 都不进仓库，结论一律回填到文档。

八份里五份是 observe——这个比例偏高。造成它的原因是每次都靠「再录一次」拿证据，
修法见[下面](#不再靠专门采集)。

## 实现分层

| 层 | 现状 | 落在哪 |
|---|---|---|
| 识别 | 15 个快速状态 + 7 个已知覆盖层 + 3 类通用清障页 | `game-states.zh-CN.json`、`play-overlays.zh-CN.json`、`obstacle-rules.zh-CN.json` |
| 能力 | 20 条能力；4 个状态刻意留空（验证码、排队中、自由落体、观战） | `config/enter-game.zh-CN.json` |
| 调度 | 幂等·toggle·commit 分级、周期触发、后置确认、环检测、过期重试丢弃 | `capabilities.py` |
| 执行 | 覆盖层先行、单一输入出口、前台校验、后置确认、留证 | `pilot.py`（2026-07-30 新增） |
| 诊断 | 输入探针、observe 干跑、未知画面自动存证 | `cli.py`、`observer.py`、`pilot.py` |

融合后的调度路径已有回归；真实 Apex 上仍要验证下面几个边界。

覆盖层没有塞进每 300ms 一次的快速识别循环：大厅、模式面板准备发输入前才扫一次，
未知画面持续 1.5 秒后才扫；10 类覆盖层共用一次全屏 OCR，再按文字块坐标恢复旧规则的
区域约束，阳性要连续命中两次才允许动作。全屏调用现在显式开启文字框检测；如果引擎
仍缺框，会降级为逐区域 OCR，而不是无限关闭安全门。欢迎/纪念页和公告只发 `ESC`，升级/奖励只发
`ENTER`，四个导览复用已有点击坐标。所有输入仍由同一个
`CapabilityDispatcher` 仲裁，OCR 失败则挡住大厅点击，而不是退化成盲点。

## 闭环的三段

一整圈是 `大厅 → 排队 → 传奇选择 → 跳伞机 → 对局 → 结算 → 大厅`，
其中需要动手的只有三段。

| 段 | 状态 | 证据 |
|---|---|---|
| 大厅 → 选模式 → 准备 → 排队 | ✅ **有实证** | `20260730-010042` 里 `ACTION_CONFIRMED lobby-start-match`，后置拿到 `LOBBY_QUEUEING` |
| 传奇选择 → 跳伞机 | ✅ 成立（不需要动手） | 约一分钟静默，符合「能自动过去的就让它过」 |
| 跳伞机脱离 | ⚠️ 键发出去了，游戏没反应 | 已改 `holdMs: 2000`，**未验** |
| 对局近战 | ✅ **输入和效果有实证** | `20260730-191304` 发出 27 次 `V`，操作者确认有近战；落地时机不作为当前闭环验收项 |
| 结算 → 大厅 | ⚠️ **完整序列已真实发出** | `20260730-191304` 自动发出 `TAB` → 等待 800ms → 长按 `SPACE` 2000ms，随后进入黑屏；为慢切页已把等待放宽到 1500ms，仍缺大厅后置确认 |

操作者已经确认最后一段必须完整执行 `TAB` 后长按 `SPACE`。它现在是一条由结算页触发的
原子动作序列：不要求先识别中间页，也不会在其他未知画面上单独发送 `SPACE`。

## 三个 bug 是同一种形态

`20260730-010042` 一次跑出三个 bug，它们的根因是同一件事：
**配置里写死的假定，和这台机器的实际情况不符。**

| bug | 假定 | 实际 |
|---|---|---|
| 训练场误按准备 | `[1280,1295]` 是「打开模式面板」 | 那一格写什么就干什么，训练大厅上写的是「准备」 |
| 近战没反应 | 近战绑在 `V` | 当时绑在 `N` |
| 跳伞没脱离 | `LCTRL` 点按 80ms 够 | 这个提示要长按 |

这三个的共同麻烦在于**它们在画面上和「输入根本没送到」长得一模一样**：动作发出去了，
截图上什么都没变。靠看截图区分不了，所以有了三样东西：

- `windows/probe-input.cmd` —— 发六个明显可见的动作，由人回答哪几个真的发生了。
  第一次跑的答案是「都有」，一句话排除了「合成输入进不去」这个方向；
- `enter-game.zh-CN.json` 的 `_keyBindings` —— 写明每个扫描码假定的是哪个键，
  有测试盯着「任何按键能力用到的码都必须在这段说明里出现过」；
- 按键能力自己的 `holdMs` —— 默认 `keyTapMs` 是给菜单点按用的，长按提示逐条声明。

## 剩余未知

| 未知 | 挡住什么 | 怎么拿到 |
|---|---|---|
| `TAB → 长按 SPACE` 返回序列能否自动回大厅 | **一局接一局** | 已实现；下次 play 以大厅后置状态直接验证 |
| 跳伞 2000ms 够不够 | 自己选落点 | 下次 play 自然验证 |
| 模式卡片 `[308,1215]` 对不对 | 从非未上榜大厅恢复 | 下次 play 自然验证 |
| 控制模式的「还活着」判据 | 控制模式里的近战 | 需要一张死亡/重生帧 |
| 修正后的登录后清障串 | 从桌面冷启动 | 本次新闻页已成为真实规则样本；OCR 缺框已有显式检测和逐区域降级，下次 play 自然验证 |

前三条不需要额外采集；冷启动串已经复用旧素材，只需要正常从「继续」起跑。控制模式
死亡/重生帧仍要在实际玩到该模式时获得，但它是扩展，不挡主线。

无法主动制造、碰到再补：断连弹窗、匹配惩罚提示（必须归入人工暂停）。

## 不再靠专门采集

主线三个待验项和冷启动清障不再要求先开一次专门的采集任务；下次正常跑 `play` 会自己
给答案。控制模式那一项则在实际遇到时由同一套自动留证补上：

- `CapabilityPilot` 现在会**给自己不认识的画面留证**：状态连续 8 秒是 `None` 就存一帧，
  每段只存一次（读条也会被拍到，这就是宽限给到 8 秒的原因）；
- 跳伞和近战的修改，在同一次跑里就会自证；
- 已知欢迎、纪念、活动、奖励、升级和四个导览已经接入 `play`，新变体认不出来也会留证；
- 状态变化本来就会存截图。

换句话说，从这一版开始，**「跑一次」和「采一次」是同一件事**。

## 下一步

`update.cmd` → `play.cmd`，从「继续」开始正常跑。已知清障页会先于大厅动作处理；
跑到结算页会直接执行 `TAB → 等待 1500ms → 长按 SPACE 2000ms`，随后以大厅态确认。
一次跑完，跳伞 2 秒、模式卡片坐标和完整返回序列会同时有答案。
